import React from 'react'
import Layout from '../Layout/Layout'

const PageNotFound = () => {
    return (
        <Layout>
            page not PageNotFound
        </Layout>
    )
}

export default PageNotFound
